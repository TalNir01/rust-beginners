# Chapter 11 - Closure

Rust *closure* is basically an anonymous function (lambda)

In most cases `rustc` automatically discover arguments & return types of closures.

Closures are like struct that can borrow elements from his `super` environment
